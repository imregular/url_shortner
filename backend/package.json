{
  "name": "url-shortner-worker",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --persist-to .wrangler/state",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply url-shortner-db --local",
    "d1:migrate:remote": "wrangler d1 migrations apply url-shortner-db --remote"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hono": "^4.7.7"
  },
  "devDependencies": {
    "wrangler": "^4.11.1"
  }
}
